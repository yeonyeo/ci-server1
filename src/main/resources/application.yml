spring:
  application:
    name: auth
  datasource:
    url: jdbc:mysql://${GATEWAY_HOST}:3307/auth_test?serverTimezone=Asia/Seoul
    username: root
    password: 1q2w3e4r

  jpa:
    hibernate:
      ddl-auto: create
